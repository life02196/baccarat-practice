# Runtime dependencies for demos and screen-based card recognition.
# ASCII-only comments keep this file compatible with older Windows pip versions.
Pillow>=9.0
numpy>=1.24
opencv-python>=4.8,<5

# Development/build dependency (installed separately by the build script):
# pyinstaller>=6.0
